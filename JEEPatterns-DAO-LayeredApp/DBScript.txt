
  CREATE TABLE "SYSTEM"."DAO_CUSTOMER" 
   (	"CID" NUMBER(15,0) NOT NULL ENABLE, 
	"CNAME" VARCHAR2(20 BYTE), 
	"CADD" VARCHAR2(20 BYTE), 
	"BILLAMT" FLOAT(126), 
	 CONSTRAINT "DAO_CUSTOMER_PK" PRIMARY KEY ("CID"));
	 
	 
	  CREATE SEQUENCE  "SYSTEM"."DP_CID_SEQ"  MINVALUE 1 MAXVALUE 1000 INCREMENT BY 1 START WITH 1 CACHE 20 NOORDER  NOCYCLE ;